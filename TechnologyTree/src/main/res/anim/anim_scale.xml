<?xml version="1.0" encoding="utf-8"?>
<scale xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="700"
    android:fromXScale="0.0"
    android:fromYScale="0.0"
    android:pivotX="50%"
    android:pivotY="50%"
    android:startOffset="0"
    android:toXScale="1.5"
    android:repeatCount="1"
    android:repeatMode="restart"
    android:toYScale="1.5" />

    <!--
        渐变尺寸动画：
            fromXScale,fromYScale  动画开始前X,Y的缩放比如：0.0为不显示， 1.0为正常大小，0.5代表起始缩小一倍，2.0代表放大一倍
            toXScale，toYScale， 动画最终缩放的倍数， 1.0为正常大小，大于1.0放大
            duration，一次动画效果消耗的时间，单位毫秒，值越小动画速度越快
            pivotX， pivotY 动画起始位置有如下三种值:
                        pivotX=50时: 表示在当前view的左上角，即原点处加上50个像素
                         pivotX=50%时: 表示在当前控件的左上角加上自己宽度的50%
                         pivotX=50%p 表示在当前控件的左上角加上父控件宽度的50%作为起点
            interpolator 指定动画插入器
            repeatCount，动画重复的计数，动画将会执行该值+1次
            repeatMode，动画重复的模式，reverse为反向，当第偶次执行时，动画方向会相反。restart为重新执行，方向不变
            startOffset， 动画多次执行的间隔时间，如果只执行一次，执行前会暂停这段时间，单位毫秒
            fillAfter 动画完成后保持动画介绍的状态
            fillBefore 动画完成后保持动画开始前状态
    -->